# 知行旅行助手简易改进方案

## 一、改进目标

在保留现有 LangGraph、FastAPI、PostgreSQL 和多 Agent 基础的前提下，将项目从课程演示原型改造成一个可验证、可持续扩展的旅行规划助手。

核心原则：

- 稳定知识使用文档 RAG。
- 天气、交通、酒店等实时信息使用 API/MCP。
- 用户偏好和旅行历史使用长期记忆。
- 预算、状态转换和订单使用确定性代码。
- 所有外部信息保留来源、查询时间和有效期。

## 二、推荐架构

```text
用户问题
   ↓
Travel Agent / LangGraph 工作流
   ├─ 用户记忆：偏好、预算、历史旅行
   ├─ 静态 RAG：目的地攻略、文化、美食、住宿区域
   ├─ 实时工具：天气、航班、火车、酒店、地图
   └─ 确定性服务：预算计算、日期校验、订单状态
            ↓
     证据整理与结果验证
            ↓
  带来源和更新时间的旅行方案
```

## 三、第一阶段：完成现有功能

### 1. 接通目的地 RAG

将 `destination_router.py` 和 `food_router.py` 中的模拟结果替换为真实检索。

推荐流程：

```text
用户问题 → BM25 + 向量召回 → RRF 融合 → Reranker → 返回证据
```

需要同步完成：

- 为每个切片生成稳定的 `chunk_id`，不要使用 Python 对象 ID 去重。
- 保存父文档，检索子切片后返回对应父文档内容。
- 实现真正的 Reranker，而不是直接截取前三条。
- 返回文档来源、标题和相关度。

### 2. 接通已有 MCP

启用天气和搜索 MCP 配置，并将 MCP 工具加入 Travel Agent。

优先替换：

- 模拟天气。
- 模拟自驾路线。
- 模拟航班和高铁。
- 模拟酒店。

如果暂时没有可靠 API，应明确返回“当前没有实时数据”，不能生成看似真实的价格或班次。

### 3. 建立统一证据格式

所有 RAG、搜索和实时 API 统一返回：

```json
{
  "content": "信息正文",
  "source": "数据来源",
  "source_url": "https://example.com",
  "retrieved_at": "2026-07-18T10:00:00+08:00",
  "valid_until": "2026-07-18T11:00:00+08:00",
  "confidence": 0.95
}
```

最终回答应显示来源和更新时间。

## 四、第二阶段：完善用户记忆

现有项目已经能够读取用户画像，但缺少稳定的写入流程。

在以下节点写入经过用户确认的记忆：

- 需求确认：旅行风格、预算和特殊需求。
- 住宿确认：住宿类型和每晚预算。
- 餐饮确认：口味、偏好和饮食禁忌。
- 行程结束：去过的城市和景点。

记忆应包含来源、创建时间和最后确认时间，并允许用户查看、修改和删除。

不要保存模型自行推测的偏好。

## 五、第三阶段：重构 Agent 编排

将核心流程改成“确定性工作流 + 局部 Agent”：

- 日期、人数、预算校验由普通 Python 函数处理。
- 阶段跳转由 StateGraph 明确控制。
- 推荐、解释和信息综合交给 LLM。
- 航班、高铁、自驾和天气作为普通工具，不必各自包装成 Agent。
- 目的地深度研究等复杂任务可以保留独立 Subagent。

在目的地确认、预算确认和订单生成前加入 LangGraph Interrupt，让用户能够批准、修改或拒绝后再继续。

## 六、知识库方案

暂时不建设完整 Wiki 或 GraphRAG，继续使用 Markdown 目的地资料即可。

建议为每篇资料添加元数据：

```yaml
---
type: destination
destination: 西安
topics: [历史, 美食, 亲子]
source_urls:
  - https://example.com
verified_at: 2026-07-18
review_after: 2026-10-18
---
```

适合存入知识库的内容：

- 城市和景点介绍。
- 历史文化背景。
- 住宿区域特点。
- 美食类别。
- 典型行程和旅行注意事项。

不适合存入长期知识库的内容：

- 实时天气。
- 航班和车次。
- 酒店库存和价格。
- 道路拥堵。
- 临时闭馆信息。

实时结果使用 Redis 设置短期缓存。

## 七、测试与验收

建立至少 20 个固定测试问题，覆盖：

- 目的地攻略能否命中正确文档。
- 天气问题是否调用天气工具。
- 航班问题是否调用交通工具。
- 预算计算是否正确。
- 信息不足时是否继续询问。
- 过期信息是否被拒绝或重新查询。
- 回答是否包含正确来源。
- 订单生成前是否等待用户确认。

测试不应只打印结果，需要使用断言验证工具选择、状态变化和返回结构。

## 八、实施优先级

1. 接通现有 RAG，修复父子切片、RRF 和 Reranker。
2. 接通天气、搜索和交通 MCP/API，移除模拟数据。
3. 统一 Evidence 证据格式，增加来源和有效期。
4. 完成用户记忆写入、修改和删除闭环。
5. 使用显式 StateGraph 和 Interrupt 加强流程控制。
6. 建立离线评测集和回归测试。
7. 数据规模扩大后，再评估 Deep Research、Wiki 和 GraphRAG。

## 九、预期结果

完成以上改进后，系统应具备：

- 可追溯的旅行推荐。
- 真实且有时效性的外部数据。
- 跨会话用户偏好记忆。
- 可暂停、确认和恢复的旅行规划流程。
- 可量化评估的检索与 Agent 质量。
- 清晰的数据边界，便于后续扩展。
