# 知行旅行助手 AI 项目交接说明

> 用途：将本文件交给新的 AI 或开发者，使其快速理解项目背景、已确认决策、技术路线和后续工作。
>
> 更新日期：2026-07-18

## 一、项目位置与目标

- 项目目录：`D:\Desktop\project\Trip`
- 项目名称：知行旅行助手
- 当前形态：Python + FastAPI + LangGraph 的国内旅行规划系统
- 改造方式：在现有代码基础上渐进式重构，不整体重写
- 首版目标：只提供国内旅行规划与推荐，不执行任何交易操作

首版需要完成目的地研究、交通建议、住宿建议、景点安排、美食推荐、天气查询、预算摘要和完整行程生成。

## 二、用户已经确认的产品决策

以下内容已经与用户确认，新的 AI 不应重复询问，除非代码实际情况与这些决策发生冲突：

| 事项 | 已确认决策 |
|---|---|
| 首版能力 | 只做规划与推荐 |
| 服务区域 | 主要服务国内旅行 |
| 交易能力 | 不做购票、酒店预订、支付、退款、取消和改签 |
| 开发方式 | 在现有代码基础上渐进式替换 |
| 用户认证 | 保留现有用户、会话和 JWT 认证，不新增复杂账户功能 |
| 旧 Agent | 迁移期保留为兼容回退，首版禁用交易工具 |
| 前端 | 首版保持兼容，下一版再考虑重构 |
| 模型约束 | 当前不考虑指定模型供应商、单次成本上限和响应时间目标 |
| 敏感数据 | 用户精确位置、订单、证件等保存范围放到下一版考虑 |
| 行程粒度 | 以天为主，细化到上午、下午、晚上或主要时间段 |
| 城市范围 | 全国目的地可输入，优先验证热门城市 |
| 数据源 | 通过统一适配器接入国内可信数据源，不绑定单一供应商 |

## 三、已确定的技术路线

### 1. 多 Agent

- 首选架构：Deep Agents 架构思想。
- 核心协作：Supervisor + Agents-as-tools。
- 执行方式：Planner-Worker，并行执行无依赖的研究任务。
- 可靠性：LangGraph 持久化状态、事件流、Interrupt 和人工审批。
- 专业 Worker：目的地、交通、酒店、美食、天气。

### 2. 广义 RAG

正式选择：

```text
MCP / Source-native RAG
        +
Deep Research
        +
Agent Memory
        +
轻量 Temporal Context
```

- 静态知识：BM25 + Embedding + RRF + Reranker。
- 实时信息：通过 MCP/API 直接查询来源系统，不永久写入静态向量库。
- 复杂研究：Deep Research 多源检索、冲突检查和引用汇总。
- 时间上下文：记录来源、查询时间、有效起止时间和置信度；过期后重新查询。

### 3. 记忆系统

- 短期状态：LangGraph Checkpointer。
- 精确用户事实：PostgreSQL。
- 语义经历和选择理由：Mem0。
- 只保存用户明确确认的长期偏好。
- 模型推测只能作为低置信度候选，未经确认不能进入正式用户画像。

### 4. 下一版候选

- AG-UI：标准化 Agent 与前端之间的事件、共享状态、中断和用户纠正。
- MCP Apps / A2UI：交互式行程地图、酒店对比卡、预算面板和审批表单。
- 当前只列入“下一版考虑”，不是首版必选依赖。

## 四、首版审批规则

### 必须审批

- 新增长期记忆。
- 修改或删除用户已经确认的长期记忆。
- 覆盖用户已经保存的正式行程。
- 向外部平台发送或分享用户数据。
- 将来使用精确实时位置时，首次获取和超出既定用途的使用。

### 无需审批

- 查询天气、景点、交通、酒店等公开信息。
- 执行 RAG、Deep Research 和只读 MCP/API 工具。
- Agent 内部规划、任务拆分和并行研究。
- 生成、比较和修改尚未保存的行程草稿。
- 保存临时会话状态和任务进度。

### 下一版如加入执行能力后必须审批

- 预订、支付、退款、取消和改签。
- 提交身份证、护照、手机号等敏感信息。
- 代表用户联系商家或发送外部消息。
- 任何产生费用、外部副作用或不可逆结果的操作。

核心原则：**只读查询默认执行；长期记忆写入、外部发送和覆盖用户数据必须确认。**

## 五、当前代码现状

项目目前已有：

- FastAPI 应用和 `/api/v1` 接口。
- 用户、会话、消息模型和 JWT 认证。
- SSE 流式聊天接口。
- LangGraph Agent、PostgreSQL Checkpointer 和 Store。
- BM25 + Dense + RRF 混合检索框架。
- ChromaDB、PostgreSQL、pgvector 和 Redis 依赖。
- MCP 客户端框架及天气、搜索服务骨架。
- 交通领域的 train、flight、driving 和 coordinator 子 Agent。
- 目的地 Router 和部分测试。

当前主要问题：

- 主流程仍是一个 Handoffs Agent 加阶段工具，不是目标 Supervisor + Workers 架构。
- `generate_order_tool` 当前位于 Agent 工具集合中，与首版只规划不交易的范围冲突，改造时必须从可调用工具中移除。
- MCP 配置仍为空或注释状态，需要实际数据源适配。
- 当前 SSE 主要发送 `token`、`tool_call`、`done` 和 `error`，尚未覆盖任务、Worker、证据和审批事件。
- RAG 已有 BM25、Dense 和 RRF，但需要稳定文档 ID、Reranker、Evidence 和时间有效性管理。
- Mem0、Deep Research、完整审批流和持久化事件日志尚未实现。

## 六、目标执行流程

```text
用户提出国内旅行需求
  ↓
读取用户已经确认的相关偏好
  ↓
Supervisor 判断任务和缺失信息
  ↓
Planner 生成带依赖关系的结构化研究任务
  ↓
目的地、交通、酒店、美食和天气 Worker 并行查询
  ↓
MCP / RAG / Deep Research 返回结构化 Evidence
  ↓
检查来源、信息冲突、查询时间和有效期
  ↓
Supervisor 生成可编辑行程草稿
  ↓
用户确认后保存正式行程和允许写入的长期记忆
```

## 七、公共数据契约方向

### Evidence 最低字段

```json
{
  "content": "证据正文",
  "source": "来源名称",
  "source_url": "https://example.com",
  "retrieved_at": "2026-07-18T10:00:00+08:00",
  "valid_until": "2026-07-18T11:00:00+08:00",
  "confidence": 0.95,
  "metadata": {}
}
```

### Worker 统一返回

- `status`：`completed`、`partial` 或 `failed`。
- `summary`：简短结论。
- `options`：结构化候选项。
- `evidence`：Evidence 列表。
- `warnings`：缺失、冲突、过期和降级说明。

### 人工审批结果

- `approve`：批准并继续。
- `edit`：使用用户修改后的参数继续。
- `reject`：取消动作并保留草稿。

### SSE 目标事件

- `task`
- `plan`
- `worker`
- `evidence`
- `approval`
- `token`
- `result`
- `error`
- `done`

需要保持旧客户端对 `token` 和 `done` 的兼容。

## 八、建议实施顺序

1. 为现有 API、认证、会话和旅行流程补充回归测试。
2. 将订单及其他交易工具从首版 Agent 工具集合中移除。
3. 定义 Pydantic 数据契约：旅行需求、研究任务、Evidence、WorkerResult、行程和审批。
4. 建立 Supervisor 和 Planner-Worker 新主流程。
5. 将目的地、交通、酒店、美食和天气封装为 Agents-as-tools。
6. 升级 Hybrid RAG、MCP、Reranker 和时间有效性检查。
7. 加入 Deep Research 和多来源引用。
8. 接入 Mem0 + PostgreSQL 用户画像，并实现记忆确认机制。
9. 加入持久化事件流、Interrupt、审批和任务恢复。
10. 统一 SSE 事件并补充任务状态、审批和行程读取接口。
11. 使用 LangSmith 建立检索、工具选择、Agent 轨迹、记忆和最终回答评测。
12. 首版稳定后再验证 AG-UI + MCP Apps / A2UI。

## 九、测试与验收重点

- 国内旅行请求能拆分成多个独立研究任务并并行执行。
- 最终行程以天和主要时间段组织。
- 天气、营业时间、交通和价格等实时信息能够追溯来源。
- 过期信息会重新查询，不直接进入最终答案。
- 数据源失败时明确降级，不伪造实时价格或余量。
- 未确认的偏好不会进入长期记忆。
- 任务中断后可以从 Checkpointer 恢复。
- 不同用户的会话、行程和记忆保持隔离。
- 首版任何路径都不能调用下单、支付或预订工具。
- 现有用户、会话、聊天历史和 SSE 接口保持兼容。

## 十、协作和问题处理约定

新的 AI 在继续工作时必须遵守：

1. 先阅读本文件、`技术栈.md`、`项目改造计划书.md`、`README.md` 和实际代码。
2. 不要整体重写项目，按阶段渐进式修改并保留可回退能力。
3. 不要直接复制完整 GitHub 项目；优先使用官方包、官方文档和最小示例。
4. 遇到技术不确定性时，先核对官方文档、官方仓库、版本说明、类型定义和测试。
5. 先用最小验证确认关键 API 和异步行为，再接入主流程。
6. 发现原技术理解错误时，明确说明原理解、实际情况、影响范围和调整方案。
7. 技术事实可以通过代码和官方资料确认的，应自行调查；涉及产品范围、隐私、用户体验或高风险操作时必须先询问用户。
8. 有不懂或会改变项目方向的地方一定先问，不得凭猜测扩大范围。
9. 不得覆盖或删除用户已有修改；修改前检查 Git 状态和项目约束。
10. 每完成一个阶段都运行相应测试，并如实报告通过、失败和未验证项。

## 十一、相关文件

- `D:\Desktop\project\Trip\技术栈.md`：已确定技术选择及下一版候选。
- `D:\Desktop\project\Trip\项目改造计划书.md`：完整实施阶段、接口和测试计划。
- `D:\Desktop\project\Trip\改进方案.md`：较早的简化改进建议，可作为背景参考。
- `D:\Desktop\project\Trip\README.md`：当前项目启动方式和既有技术栈。

## 十二、交接后的第一步

新的 AI 接手后应先进行只读检查：

1. 查看 Git 状态，识别用户已有修改和未跟踪文件。
2. 阅读 FastAPI 入口、Agent 主流程、状态定义、聊天 SSE、RAG 和 MCP 客户端。
3. 运行不修改代码的现有测试，记录当前基线。
4. 将实际代码与《项目改造计划书》的阶段 0 对照。
5. 向用户报告基线结果和第一批准备修改的内容，再开始代码变更。

当前尚未按照新计划修改业务代码；已经完成的是技术路线、实施计划和交接文档。
