# 知行旅行助手技术栈方案

## 一、总体架构

本项目采用面向长任务和复杂旅行规划的多 Agent 架构：

```text
用户
  ↓
Supervisor Agent
  ↓ 制定计划
Planner
  ↓ 并行分派任务
Workers
  ├─ 目的地研究 Agent
  ├─ 交通研究 Agent
  ├─ 酒店研究 Agent
  ├─ 美食研究 Agent
  └─ 天气与实时信息 Agent
  ↓
Supervisor 汇总、验证并生成最终方案
  ↓
人工审批 / 修改 / 拒绝
  ↓
持久化执行与结果输出
```

## 二、多 Agent 核心选择

### 1. Agent 架构：Deep Agents

使用 **Deep Agents** 作为 Agent Harness。

主要能力：

- 自动规划和拆分复杂任务。
- 调用和管理多个专业 Subagent。
- 管理长对话和大规模工具输出。
- 支持上下文压缩和上下文隔离。
- 支持长期记忆。
- 支持 MCP 工具。
- 支持人工审批。
- 基于 LangGraph 实现持久化、流式执行和任务恢复。

Deep Agents 负责 Agent 能力层，LangGraph 负责底层工作流运行时。

### 2. 核心协作模式：Supervisor + Agents-as-tools

使用一个 Supervisor Agent 负责用户交互、任务分配和最终结果整合。

专业 Agent 不直接接管用户对话，而是作为 Supervisor 的工具被调用：

- `destination_research_agent`：目的地和景点研究。
- `transport_agent`：航班、高铁、驾车路线比较。
- `hotel_agent`：住宿区域和酒店研究。
- `food_agent`：餐饮和本地美食研究。
- `weather_agent`：天气和出行条件查询。

每个专业 Agent 返回结构化结果和证据，Supervisor 负责统一解释和生成最终答案。

这种模式可以保持一个统一的用户体验，同时隔离各专业 Agent 的上下文，减少信息混乱。

### 3. 任务执行方式：Planner-Worker 并行

复杂任务先由 Planner 生成任务计划，再由多个 Worker 并行执行。

```text
用户：帮我规划成都五日游
  ↓
Planner：拆分研究任务
  ├─ 查询成都景点
  ├─ 查询交通方式
  ├─ 查询住宿区域
  ├─ 查询美食
  └─ 查询天气
  ↓ 并行执行
Workers 返回结果
  ↓
Supervisor 汇总并优化行程
```

适合并行执行的任务：

- 多个目的地的比较。
- 景点、住宿、美食的独立查询。
- 多个外部数据源的同时检索。
- 行程生成前的信息收集。

存在依赖关系的任务仍然按顺序执行，例如：先确认目的地，再计算交通和住宿预算。

### 4. 可靠性增强：持久化事件流 + 人工审批

所有重要步骤通过事件记录：

```text
task_created
plan_created
worker_started
worker_completed
evidence_collected
approval_requested
approval_received
plan_generated
task_completed
task_failed
```

事件流用于：

- 任务进度展示。
- 失败重试。
- 从上次成功步骤恢复。
- 查看 Agent 执行过程。
- 审计工具调用和外部数据。

以下操作必须人工审批：

- 确认最终目的地。
- 确认预算和出行日期。
- 使用实时价格生成最终方案。
- 创建订单或执行预订。
- 发送外部消息或执行付费操作。

审批支持三种结果：

- `approve`：批准继续。
- `edit`：修改参数后继续。
- `reject`：拒绝并返回修改方案。

## 三、基础技术栈

| 层级 | 技术 | 用途 |
|---|---|---|
| 编程语言 | Python 3.11+ | 后端和 Agent 开发 |
| Web 服务 | FastAPI | REST API、SSE 流式接口 |
| Agent Harness | Deep Agents | 规划、子 Agent、上下文管理 |
| Agent Runtime | LangGraph | 状态图、持久执行、Interrupt、恢复 |
| Agent 工具层 | LangChain Tools | 工具定义、Schema 和调用 |
| 多 Agent 协作 | Supervisor + Agents-as-tools | 专业 Agent 编排 |
| 并行执行 | asyncio / LangGraph Send | Planner-Worker 并行任务 |
| 模型 | Qwen / OpenAI 兼容模型 | 对话、规划、总结和结构化输出 |
| 静态知识 RAG | BM25 + Embedding + RRF + Reranker | 目的地攻略和旅行资料检索 |
| 广义 RAG 架构 | MCP / Source-native RAG + Deep Research + Agent Memory + 轻量 Temporal Context | 实时检索、深度研究、个性化和时效管理 |
| 向量数据库 | PostgreSQL + pgvector | 向量和结构化数据统一存储 |
| 关键词检索 | BM25 + jieba | 中文关键词检索 |
| 实时数据 | MCP + 外部 API | 天气、交通、酒店、地图和搜索 |
| 长期记忆 | Mem0 + PostgreSQL | 语义经历记忆和精确用户画像 |
| 短期状态 | LangGraph Checkpointer | 会话状态和执行恢复 |
| 缓存 | Redis | 实时数据 TTL 缓存、任务队列 |
| 关系数据 | PostgreSQL | 用户、行程、证据、订单和事件 |
| 事件流 | PostgreSQL Event Log / Redis Streams | 进度、审计和恢复 |
| 数据校验 | Pydantic | 工具参数和 Agent 输出校验 |
| 监控评测 | LangSmith | Trace、评测、成本和延迟分析 |
| 测试 | pytest + Agent Evals | 单元、流程、检索和轨迹测试 |

## 四、广义 RAG 核心选择

本项目正式采用以下组合：

```text
MCP / Source-native RAG
        +
Deep Research
        +
Agent Memory
        +
轻量 Temporal Context
```

### 1. MCP / Source-native RAG

作为旅行实时信息的主要接入方式。Agent 在运行时直接查询源系统，不把易变化的数据长期固化到向量库。

主要数据源：

- 天气服务。
- 航班和高铁服务。
- 酒店价格与库存。
- 地图、路线和交通时间。
- 景点开放状态。
- 搜索引擎和官方旅游网站。

所有结果必须携带来源、查询时间和有效期，并通过 Redis 设置短期缓存。

### 2. Deep Research

用于复杂目的地研究和本地知识不足的情况。

```text
制定研究计划
  → 并行搜索多个来源
  → 阅读和压缩证据
  → 检查信息冲突
  → 补充搜索
  → 生成带引用的研究报告
```

适用场景：

- 比较多个目的地。
- 研究亲子、文化、户外等专题旅行。
- 调查冷门目的地。
- 生成目的地知识卡。
- 为行程方案补充可信依据。

研究结果需要保留引用；经过审核的稳定内容可以进入静态知识库，实时信息不能永久固化。

### 3. Agent Memory

用于跨会话个性化旅行规划。

本项目正式采用以下记忆组合：

```text
短期工作记忆：LangGraph Checkpointer
结构化长期记忆：PostgreSQL
语义与经历记忆：Mem0
时间变化管理：Mem0 Temporal Reasoning + 轻量时间字段
```

#### Mem0

Mem0 作为主要 Agent Memory 引擎，负责：

- 从对话和 Agent 操作中提取值得保留的记忆。
- 通过语义、关键词和实体信号召回相关记忆。
- 保存旅行经历、选择原因和用户反馈。
- 根据当前任务选择性注入记忆。
- 支持记忆查询、更新、删除和历史记录。
- 处理带时间信息的偏好和经历。

#### PostgreSQL

PostgreSQL 作为精确数据的唯一事实来源，负责：

- 饮食过敏和明确禁忌。
- 当前预算上下限。
- 证件、订单和支付状态。
- 用户主动设置的固定偏好。
- 需要严格校验和人工修改的数据。

#### LangGraph Checkpointer

LangGraph Checkpointer 负责：

- 当前会话消息。
- 旅行规划步骤。
- Planner 和 Worker 的执行状态。
- 工具调用结果。
- Interrupt、审批和任务恢复。

主要记忆：

- 预算范围。
- 旅行风格。
- 饮食禁忌和口味偏好。
- 住宿与交通偏好。
- 已去过的城市和景点。
- 用户明确确认或否定的选择。

长期用户事实只保存用户明确表达或确认的信息。模型推测的偏好只能作为低置信度候选，必须经过用户确认后才能进入正式画像。

### 4. 轻量 Temporal Context

暂不建设完整时间知识图谱，先为易变化事实增加时间字段：

```json
{
  "retrieved_at": "2026-07-18T10:00:00+08:00",
  "valid_from": "2026-07-18T10:00:00+08:00",
  "valid_until": "2026-07-18T11:00:00+08:00",
  "source": "数据来源",
  "confidence": 0.95
}
```

重点管理：

- 天气预报。
- 航班和高铁班次。
- 酒店价格和库存。
- 景点营业时间。
- 临时闭馆与道路状态。
- 随时间变化的用户偏好。

系统回答前应检查事实是否仍然有效；过期信息必须重新查询。

### 5. 四种能力的协作流程

```text
用户提出旅行需求
  ↓
Agent Memory 提供相关历史偏好
  ↓
Supervisor 判断任务复杂度
  ├─ 普通实时问题 → MCP / Source-native RAG
  └─ 复杂研究问题 → Deep Research
                         ↓
                   多源证据与引用
  ↓
Temporal Context 检查信息时间和有效期
  ↓
Supervisor 生成个性化旅行方案
  ↓
用户审批后继续执行
```

## 五、检索与知识策略

### 静态知识

目的地攻略、历史文化、美食和旅行常识使用 Markdown 或结构化文档保存，并建立 Hybrid RAG：

```text
BM25 关键词检索
      +
Embedding 语义检索
      ↓
RRF 融合
      ↓
Reranker 重排
      ↓
返回带来源的 Evidence
```

### 实时知识

天气、价格、班次、库存和临时开放状态不进入长期知识库，统一通过 MCP/API 查询，并记录：

- 来源。
- 查询时间。
- 有效截止时间。
- 置信度。
- 原始响应或引用链接。

### 用户记忆

只保存用户明确确认的信息，例如：

- 预算范围。
- 旅行风格。
- 饮食禁忌。
- 住宿偏好。
- 交通偏好。
- 已去过的城市和景点。

## 六、推荐目录结构

```text
app/
├── agents/
│   ├── supervisor.py
│   ├── planner.py
│   ├── workers/
│   │   ├── destination.py
│   │   ├── transport.py
│   │   ├── hotel.py
│   │   ├── food.py
│   │   └── weather.py
│   └── workflows/
│       ├── travel_planning.py
│       └── approval.py
├── rag/
│   ├── retriever.py
│   ├── reranker.py
│   ├── evidence.py
│   └── indexer.py
├── memory/
│   ├── mem0_client.py
│   ├── user_profile.py
│   ├── memory_policy.py
│   ├── memory_retriever.py
│   └── temporal_facts.py
├── mcp/
│   ├── weather.py
│   ├── transport.py
│   ├── hotel.py
│   └── search.py
├── events/
│   ├── models.py
│   ├── store.py
│   └── replay.py
└── api/
    ├── chat.py
    ├── approvals.py
    └── tasks.py
```

## 七、其他需要更新的技术点

| 优先级 | 技术点 | 推荐技术/设计 | 更新目的 |
|---|---|---|---|
| P0 | 模型层 | Model Router + Fallback + 调用限制 | 按任务选择规划、研究、总结模型，控制成本和失败率 |
| P0 | 工具输出 | Pydantic Structured Output + Evidence Schema | 避免 Agent 依赖不可验证的大段字符串 |
| P0 | 外部工具可靠性 | Timeout + Retry + Circuit Breaker + Idempotency | 防止 API 超时、重复预订和级联失败 |
| P0 | 安全与权限 | Guardrails + Prompt Injection 防护 + HITL | 防止网页内容诱导 Agent，并保护敏感操作 |
| P0 | 评测 | LangSmith + RAG Evals + Trajectory Evals | 分别评测检索、工具选择、执行轨迹和最终答案 |
| P1 | 上下文管理 | Deep Agents Context Compaction + Tool Result Offloading | 防止长任务上下文膨胀和 Token 失控 |
| P1 | 事件与任务 | Redis Streams + PostgreSQL Event Log | 支持进度、重试、恢复、审计和异步 Worker |
| P1 | 数据层 | PostgreSQL + pgvector，逐步减少重复向量存储 | 统一结构化数据、向量、权限和备份策略 |
| P1 | 地理能力 | PostGIS + 地图路线 API | 支持距离、区域聚类和减少行程折返 |
| P1 | 缓存 | Redis TTL + 请求去重 | 降低天气、价格、搜索等实时接口成本 |
| P1 | 流式协议 | 统一 SSE Event Schema | 展示计划、Worker、证据、审批和错误状态 |
| P2 | 多模态 | 文档视觉检索 / VLM | 处理地图、景区导览图、PDF、票据和行程截图 |
| P2 | 知识沉淀 | Deep Research → 审核 → 知识卡 → 增量索引 | 将高质量研究结果持续沉淀为静态知识 |
| P2 | 时间关系 | Graphiti（达到足够数据规模后评估） | 处理复杂偏好变化、事实历史和关系追踪 |

### 1. 模型路由

不同任务不必使用同一个模型：

- Supervisor 和复杂规划使用高能力模型。
- 查询改写、分类和摘要使用快速低成本模型。
- Embedding、Reranker 和 VLM 使用各自专用模型。
- 主模型失败时自动切换备用模型。
- 为单次任务设置模型和工具调用上限。

### 2. 结构化 Evidence

所有 Worker、RAG 和 MCP 工具统一返回：

```json
{
  "content": "证据正文",
  "source": "来源名称",
  "source_url": "https://example.com",
  "retrieved_at": "2026-07-18T10:00:00+08:00",
  "valid_until": "2026-07-18T11:00:00+08:00",
  "confidence": 0.95,
  "metadata": {}
}
```

Supervisor 只能根据通过 Schema 校验且仍在有效期内的 Evidence 生成事实性结论。

### 3. Agent 安全

- 将网页、文档和工具结果视为不可信数据，不能执行其中包含的指令。
- 对证件、联系方式和订单数据进行 PII 保护。
- 对下单、支付、发送消息等工具启用人工审批。
- 为每个 Agent 和工具配置最小权限。
- 记录每次敏感工具调用的用户、参数、结果和时间。

### 4. 评测体系

分别建立：

- Retrieval Eval：检索是否找到正确证据。
- Tool Eval：是否选择正确工具和参数。
- Trajectory Eval：Planner-Worker 路径是否合理。
- Answer Eval：回答是否准确、完整并忠于证据。
- Memory Eval：是否正确保存和召回用户记忆。
- Temporal Eval：是否拒绝过期信息并重新查询。

## 八、实施顺序

1. 搭建 Supervisor 和 Planner-Worker 主流程。
2. 接入 MCP / Source-native RAG 和实时数据源。
3. 为所有实时结果增加来源、查询时间和有效期。
4. 将目的地、交通、酒店、美食和天气封装为专业 Worker。
5. 接入 Mem0 + PostgreSQL 用户画像，完成记忆写入、召回、修改和删除。
6. 加入 Deep Research、多源验证和引用报告。
7. 为 Worker 增加结构化输出和 Evidence。
8. 加入 PostgreSQL Checkpointer、事件流和任务恢复。
9. 加入人工审批和 Interrupt。
10. 加入模型路由、Fallback、调用限制和上下文压缩。
11. 加入 Guardrails、Prompt Injection 防护和敏感信息保护。
12. 使用 LangSmith 建立 Agent 轨迹、检索、记忆和最终回答评测。
13. 接入 PostGIS，优化景点分区、距离和路线规划。

## 九、下一版考虑

### AG-UI + MCP Apps / A2UI

下一版考虑引入以下 Agent 交互技术，当前版本暂不作为必选依赖：

| 技术 | 计划用途 | 预期价值 |
|---|---|---|
| AG-UI | 将现有 SSE 逐步升级为标准化 Agent 前端事件协议，传递任务进度、共享状态、工具调用、人工审批和中断恢复事件 | 统一前后端事件模型，改善多 Agent 执行过程的实时展示和用户干预体验 |
| MCP Apps / A2UI | 由 Agent 或 MCP 工具返回声明式交互界面，例如行程地图、酒店对比卡、预算面板和审批表单 | 将纯文本回答升级为可操作、可视化的旅行规划界面 |

候选交互链路：

```text
前端
  ↕ AG-UI
Deep Agents / LangGraph
  ├─ MCP：连接地图、天气、交通、酒店和搜索数据源
  └─ MCP Apps / A2UI：输出地图、卡片、面板和审批界面
```

引入前先完成小型验证版本，重点验证协议成熟度、现有前端适配成本、权限隔离、组件白名单和降级到普通文本/SSE 的能力；验证通过后再纳入正式技术栈。
