# 知行旅行助手项目改造计划书

## 一、改造目标

在现有代码基础上进行渐进式改造，将项目从“单个 Handoffs Agent 驱动的旅行流程”升级为面向国内旅行规划的多 Agent 系统。

首版只提供规划与推荐，不执行购票、酒店预订、支付、退款、取消或改签。现有 FastAPI API、用户认证、会话体系和数据库继续保留，旧 Agent 流程在迁移期间作为兼容回退路径。

首版完成后应具备：

- Supervisor + Agents-as-tools 的统一协作入口。
- Planner-Worker 并行研究和可恢复执行。
- MCP / Source-native RAG、Deep Research、Agent Memory 和轻量时间上下文。
- 带来源、查询时间和有效期的结构化 Evidence。
- 只读工具自动执行，长期记忆写入和外部数据发送需要人工确认。
- 可观测、可测试、可降级的旅行规划流程。

## 二、既有能力与处理原则

| 现有能力 | 处理方式 |
|---|---|
| FastAPI、用户、会话和 JWT 认证 | 保留，不扩展复杂账户体系 |
| SSE 流式对话接口 | 保留并统一事件格式，下一版再评估 AG-UI |
| PostgreSQL Checkpointer | 保留，继续承担会话状态和任务恢复 |
| BM25 + Dense + RRF | 保留并修正融合标识，增加 Reranker 和 Evidence |
| ChromaDB | 迁移期保留，稳定后评估统一到 PostgreSQL + pgvector |
| MCP 客户端和本地服务 | 补齐配置、超时、重试和来源元数据 |
| 单个 Handoffs Agent | 保留为临时回退路径，新请求逐步迁移到 Supervisor |
| 订单生成及交易工具 | 首版从 Agent 可用工具集中移除，不允许模型调用 |
| 当前前端 | 保持兼容，不在首版重构 |

## 三、目标架构

```text
FastAPI / SSE
  ↓
Supervisor Agent
  ├─ Planner：生成结构化研究计划
  ├─ Destination Worker
  ├─ Transport Worker
  ├─ Hotel Worker
  ├─ Food Worker
  └─ Weather Worker
       ↓
MCP / API + Hybrid RAG + Deep Research
       ↓
Evidence 校验、时间有效性检查和冲突处理
       ↓
Supervisor 汇总可编辑行程草案
       ↓
用户确认后保存正式行程和允许写入的长期记忆
```

### 核心技术选择

| 领域 | 首版方案 |
|---|---|
| Agent 架构 | Deep Agents 思路，Supervisor + Agents-as-tools |
| 执行方式 | Planner-Worker，并行执行相互独立的研究任务 |
| 运行时 | LangGraph Checkpointer、Interrupt 和任务恢复 |
| 实时检索 | MCP / Source-native RAG |
| 复杂研究 | Deep Research，多源检索、冲突检查和引用汇总 |
| 静态检索 | BM25 + Embedding + RRF + Reranker |
| 长期记忆 | Mem0 保存语义经历，PostgreSQL 保存确认后的精确偏好 |
| 时间上下文 | `retrieved_at`、`valid_from`、`valid_until` 和来源字段 |
| 事件流 | 持久化事件日志 + 统一 SSE 事件 Schema |
| 下一版候选 | AG-UI + MCP Apps / A2UI、前端重构和敏感数据治理 |

## 四、实施阶段

### 阶段 0：建立安全基线和兼容边界

- 为现有接口和核心旅行流程补充基线测试。
- 将旧 Agent 封装为可切换的兼容实现，默认保留但不继续扩展。
- 从生产工具列表移除订单、支付、预订和其他副作用工具。
- 统一配置读取，确保密钥不进入日志、状态或模型上下文。
- 定义新旧流程切换配置；新流程异常时只允许回退到规划能力，不得恢复交易工具。

验收标准：现有认证、会话、聊天历史和 SSE 接口保持可用；模型无法调用任何交易工具。

### 阶段 1：建立数据契约和 Supervisor 主流程

- 使用 Pydantic 定义旅行需求、研究任务、Evidence、候选方案、每日行程和预算摘要。
- 新建 Supervisor，负责理解需求、调用 Planner、分派 Worker、校验证据和生成最终草案。
- Planner 输出结构化任务列表，标记任务类型、依赖关系、所需工具和完成条件。
- 将现有目的地和交通能力改造成 Agents-as-tools；酒店、美食和天气按同一接口补齐。
- 无依赖任务并行执行；目的地确认、路线计算等有依赖任务按顺序执行。

验收标准：一次国内旅行请求能够生成研究计划，并并行完成至少目的地、交通、酒店、天气四类任务。

### 阶段 2：升级 RAG、MCP 和证据体系

- 保留 BM25 与向量检索，引入稳定文档标识完成 RRF 融合，并启用 Reranker。
- 所有 RAG、MCP 和外部 API 结果统一转换为 Evidence，不允许 Worker 返回不可验证的大段自由文本作为事实依据。
- Evidence 至少包含正文、来源、来源地址、查询时间、有效期、置信度和元数据。
- 为天气、营业时间、交通和价格配置不同有效期；过期信息在回答前强制重新查询。
- Deep Research 仅用于复杂目的地比较和专题研究，结果必须保留多来源引用并标记冲突。
- MCP 工具增加超时、有限重试、熔断、请求去重和失败降级；无实时结果时明确提示，不伪造数据。

验收标准：最终行程中的实时事实可以追溯到 Evidence；过期数据不会被直接用于回答。

### 阶段 3：加入记忆、审批和持久化事件流

- LangGraph Checkpointer 保存短期会话、Planner/Worker 状态和 Interrupt 恢复点。
- PostgreSQL 保存用户明确确认的预算、饮食禁忌、住宿和交通偏好。
- Mem0 保存可召回的旅行经历和选择理由；模型推测只作为候选，未经确认不得写入正式画像。
- 长期记忆新增、修改、删除，以及覆盖已保存行程、向外部发送数据时触发人工审批。
- 查询公开数据、执行 RAG、运行只读工具和修改未保存草稿无需审批。
- 持久化 `task_created`、`plan_created`、`worker_started`、`worker_completed`、`evidence_collected`、`approval_requested`、`approval_received`、`task_completed` 和 `task_failed` 事件。

验收标准：任务中断后可从最近检查点恢复；未经确认不会写入长期记忆或覆盖正式行程。

### 阶段 4：统一 API、SSE 和可观测性

- 保持现有 `/api/v1/chat/stream/{conversation_id}` 路径兼容。
- SSE 统一输出 `task`、`plan`、`worker`、`evidence`、`approval`、`token`、`result`、`error` 和 `done` 类型。
- 每个事件包含任务 ID、会话 ID、时间、顺序号和可选载荷，客户端可据此去重和恢复展示。
- 增加查看任务状态、提交审批结果和读取已保存行程的 API。
- 使用 LangSmith 记录模型、工具、检索和 Agent 轨迹；日志中隐藏密钥和用户敏感字段。
- 增加超时、错误类型和降级原因的指标。

验收标准：旧客户端仍可读取 `token` 和 `done`；新客户端能够展示 Planner、Worker、证据和审批状态。

### 阶段 5：下一版验证项

- 使用 AG-UI 验证标准化 Agent 事件、共享状态、中断和用户纠正。
- 使用 MCP Apps / A2UI 验证行程地图、酒店对比卡、预算面板和审批表单。
- 评估前端重构、用户精确位置、订单、证件等敏感数据的保存范围和权限策略。
- 验证失败时继续使用现有 SSE 和普通文本/JSON 界面，不影响首版运行。

## 五、接口与数据约定

### Evidence

```json
{
  "content": "证据正文",
  "source": "来源名称",
  "source_url": "https://example.com",
  "retrieved_at": "2026-07-18T10:00:00+08:00",
  "valid_until": "2026-07-18T11:00:00+08:00",
  "confidence": 0.95,
  "metadata": {}
}
```

### Worker 输出

每个 Worker 统一返回：

- `status`：`completed`、`partial` 或 `failed`。
- `summary`：简短研究结论。
- `options`：结构化候选项。
- `evidence`：支撑结论的 Evidence 列表。
- `warnings`：冲突、缺失和过期风险。

### 审批结果

- `approve`：按当前内容继续。
- `edit`：使用用户修改后的结构化参数继续。
- `reject`：取消当前写入或外部发送动作，保留草稿。

## 六、测试计划

### 单元测试

- Planner 任务拆分、依赖关系和并行分组。
- Worker 输入输出 Schema 校验。
- RRF、Reranker、Evidence 转换和时间有效性判断。
- 记忆候选、确认写入、修改和删除策略。
- SSE 事件序号、序列化和错误事件。

### 集成测试

- Supervisor 调用多个 Worker 并汇总国内旅行方案。
- MCP/API 超时、重试、熔断和静态知识降级。
- Checkpointer 中断恢复和事件重放。
- 用户会话归属、行程归属和跨用户隔离。
- 人工审批的 `approve`、`edit`、`reject` 三条路径。

### 安全与回归测试

- 网页和文档中的提示注入不能触发工具或改变系统指令。
- 订单、支付、预订工具不在首版可调用集合中。
- 未确认的模型推测不会写入长期记忆。
- 现有用户、会话、历史消息和 SSE 客户端保持兼容。

### 首版验收场景

用户输入“从上海出发，预算 6000 元，规划成都五日文化美食游”后，系统应：

1. 提取并展示结构化需求。
2. 并行研究目的地、交通、酒店、天气和美食。
3. 返回上午、下午、晚上粒度的五日行程。
4. 为天气、营业时间和交通信息提供来源与查询时间。
5. 对无法确认的实时价格明确标注，不生成虚假余量。
6. 允许用户修改草稿；只有确认后才保存正式行程和长期偏好。
7. 全程不提供下单、支付或自动联系商家的执行能力。

## 七、默认决策

- 采用渐进式替换，不整体重写项目。
- 保留现有认证和 `user_id` 数据隔离，不扩展复杂账户功能。
- 旧 Handoffs Agent 只作迁移期回退，完成新流程验收后停止使用。
- 首版面向国内旅行，全国目的地可输入，优先验证热门城市。
- 行程按天和上午/下午/晚上生成，不追求分钟级安排。
- 当前前端保持兼容；AG-UI + MCP Apps / A2UI 仅列入下一版验证。
- 暂不以模型供应商、单次成本和响应时间作为首版设计约束。
- 首版不保存证件、支付信息、订单信息和精确实时位置。
